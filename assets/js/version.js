// Single build stamp for the app shell. Must match VERSION in ../../service-worker.js
// (that file also carries the package version prefix, e.g. "petal-reader-v1.0.0-…" —
// this is just the date-topic suffix, matching every other app's APP_BUILD).
export const APP_BUILD = "2026.09.07-librarycare";
